What I want:
* Will be an interactive CLI. The first prompt to the user will be about the mode they want to use. The modes are:
  * Manual mode: The cli will provide the prompt that the user will use to generate the evaluator question from the an external chatbot of preference. After enter, the user can paste the text of the question and press enter to continue.
  * Automatic mode: The cli will generate the evaluator question itself using a model
* The result will be saved in judge.txt